# 温控风扇自启动系统 — 调试与测试指南 v2.0

> STM32F103C8T6 + LoRa SX1278 + HUB12 P10 点阵屏 | 2026年3月

---

## 目录

1. [项目建设流程](#1-项目建设流程)
2. [从机测试流程](#2-从机测试流程)
3. [主机测试流程](#3-主机测试流程)
4. [联调测试流程](#4-联调测试流程)
5. [完整功能测试](#5-完整功能测试)
6. [引脚冲突检查](#6-引脚冲突检查)
7. [常见问题排查](#7-常见问题排查)
8. [测试记录表](#8-测试记录表)

---

## 1. 项目建设流程

### 1.1 准备阶段

| 步骤 | 操作 | 检查项 |
|------|------|--------|
| 1.1 | 采购器件 (参照项目技术文档第 3 节) | 全部器件到齐, 无缺件 |
| 1.2 | 检查 STM32 最小系统板 | USB 供电后 LED 亮, 可被 ST-Link 识别 |
| 1.3 | 检查 SX1278 模块 | 接线正确, 天线完好 |
| 1.4 | 检查 P10 点阵屏 (HUB12 接口) | 8P 排针无虚焊, 5V 供电后部分 LED 微亮属正常 |
| 1.5 | 准备工具 | 万用表、杜邦线、面包板或洞洞板、ST-Link V2 |

### 1.2 从机组装

| 步骤 | 操作 | 注意事项 |
|------|------|----------|
| 2.1 | 焊接/插接 DS18B20 → PA0 | DQ 引脚接 4.7kΩ 上拉至 3.3V |
| 2.2 | 焊接/插接 DHT22 → PA1 | DATA 引脚接 10kΩ 上拉至 3.3V |
| 2.3 | 插接 LoRa SX1278 → SPI1 | PA4=NSS, PA5=SCK, PA6=MISO, PA7=MOSI, PB0=DIO0 |
| 2.4 | 插接继电器模块 → PB1 | 5V 供电, IN 接 PB1, GND 共地 |
| 2.5 | 连接电源 | 5V/2A 适配器, GND 共地 |
| 2.6 | **先不接 220V** — 仅测试 5V 控制部分 | 继电器动作时应听到"嗒"声 |

### 1.3 主机组装

| 步骤 | 操作 | 注意事项 |
|------|------|----------|
| 3.1 | 插接 LoRa SX1278 → SPI1 | 与从机相同接法 |
| 3.2 | 插接 HUB12 P10 点阵屏 | PB3=A, PB4=B, PB5=CLK, PB6=STB, PB7=OE, PB8=R1, 5V+GND |
| 3.3 | 插接 4 个按键 | KEY1→PC13, KEY2→PA8, KEY3→PB11, KEY4→PB10, 另一端接 GND |
| 3.4 | 连接电源 | 5V/2A 适配器 (点阵屏功耗约 1~2A) |

### 1.4 软件环境

| 步骤 | 操作 |
|------|------|
| 4.1 | 安装 Keil MDK-ARM v5.x |
| 4.2 | 安装 STM32F1xx DFP (器件支持包) |
| 4.3 | 从机工程: 添加 `common/src/*.c` + `slave/src/*.c` |
| 4.4 | 从机工程: Include 路径 → `common/inc`, `slave/inc` |
| 4.5 | 从机工程: 宏定义 → `STM32F10X_MD`, `USE_STDPERIPH_DRIVER` |
| 4.6 | 主机工程: 添加 `common/src/*.c` + `master/src/*.c` |
| 4.7 | 主机工程: Include 路径 → `common/inc`, `master/inc` |
| 4.8 | 主机工程: 宏定义 → `STM32F10X_MD`, `USE_STDPERIPH_DRIVER` |
| 4.9 | 两个工程的 Debug 选项卡: 选择 ST-Link, SWD 模式 |
| 4.10 | 编译 — 0 Error, 0 Warning |

### 1.5 烧录

| 步骤 | 操作 |
|------|------|
| 5.1 | ST-Link 连接从机: SWDIO, SWCLK, GND (3 线) |
| 5.2 | Keil 按 F8 (Download) 烧录从机程序 |
| 5.3 | ST-Link 连接主机: 同上 |
| 5.4 | 烧录主机程序 |

---

## 2. 从机测试流程

> **测试环境**: 仅从机单独上电, 不连接主机

### 2.1 电源测试

| 序号 | 测试项 | 操作 | 预期结果 | 通过? |
|------|--------|------|----------|-------|
| S1 | 电源电压 | 万用表测量 STM32 3.3V 引脚 | 3.2~3.4V | ☐ |
| S2 | 电源电压 | 万用表测量继电器模块 VCC | 4.8~5.2V | ☐ |
| S3 | LED 指示 | 观察 STM32 板上 LED | 上电后 LED 亮 (如有) | ☐ |

### 2.2 DS18B20 温度传感器测试

| 序号 | 测试项 | 操作 | 预期结果 | 通过? |
|------|--------|------|----------|-------|
| S4 | 初始化检测 | 在 `main()` 的 `App_Slave_Init()` 之后加断点, 检查 `DS18B20_Init()` 返回值 | 不死循环 (传感器有响应) | ☐ |
| S5 | 常温读取 | 等待 2 秒, 读取 `_state.temperature` | 室温值 (20~30°C) | ☐ |
| S6 | 升温响应 | 手握住 DS18B20 探头 | 温度逐渐上升 (>0.5°C) | ☐ |
| S7 | 降温响应 | 松开探头, 等待 30 秒 | 温度逐渐回落到室温 | ☐ |
| S8 | 无传感器 | 拔掉 DS18B20, 重新上电 | 返回 -100.0 (可检测故障) | ☐ |

### 2.3 DHT22 温湿度传感器测试

| 序号 | 测试项 | 操作 | 预期结果 | 通过? |
|------|--------|------|----------|-------|
| S9 | 读取成功 | 上电 3 秒后, 检查 `_state.humidity` | 湿度值 (30~80%RH) | ☐ |
| S10 | 湿度响应 | 对 DHT22 呵气 | 湿度值明显上升 (>5%) | ☐ |
| S11 | 校验正确 | 连续读取 10 次 | 不出现校验失败 | ☐ |

### 2.4 继电器测试

| 序号 | 测试项 | 操作 | 预期结果 | 通过? |
|------|--------|------|----------|-------|
| S12 | 闭合 | 在调试器中调用 `Relay_On()` | 继电器"嗒"一声, LED 指示灯亮 | ☐ |
| S13 | 断开 | 调用 `Relay_Off()` | 继电器"嗒"一声, LED 熄灭 | ☐ |
| S14 | 默认状态 | 重新上电 (不烧录新程序) | 继电器默认断开 (安全状态) | ☐ |
| S15 | GPIO 电平 | 万用表测 PB1 (继电器 ON 时) | 3.3V | ☐ |
| S16 | GPIO 电平 | 万用表测 PB1 (继电器 OFF 时) | 0V | ☐ |

### 2.5 LoRa 模块测试 (从机)

| 序号 | 测试项 | 操作 | 预期结果 | 通过? |
|------|--------|------|----------|-------|
| S17 | SPI 通信 | 在 `SX1278_Init()` 后读取版本号 | 返回 0x12 | ☐ |
| S18 | 初始化成功 | 检查 `SX1278_Init()` 返回值 | `SX1278_OK` | ☐ |
| S19 | 进入接收 | 调用 `SX1278_StartRx()` | 不死循环, DIO0 引脚可用 | ☐ |
| S20 | 发送数据 | 手动调用 `SX1278_Send()` | 发送完成 (DIO0 中断触发) | ☐ |

> ⚠️ **S17 返回非 0x12 常见原因**: SPI 接线错误、模块未供电、模块损坏

---

## 3. 主机测试流程

> **测试环境**: 仅主机单独上电, 不连接从机

### 3.1 HUB12 点阵屏测试

| 序号 | 测试项 | 操作 | 预期结果 | 通过? |
|------|--------|------|----------|-------|
| M1 | 屏幕点亮 | 上电后观察屏幕 | 显示 "TEMP CTRL V2.0 WAIT..." | ☐ |
| M2 | 全亮测试 | 调用 `HUB12_Clear()` 后逐像素点亮 | 所有 512 个像素可控 | ☐ |
| M3 | 字符显示 | 调用 `HUB12_DrawString(0, 0, "ABC123")` | 正确显示 ASCII 字符 | ☐ |
| M4 | 行显示 | 调用 `HUB12_PrintLine(0, "TEST")` | 第一行显示文本, 无残留 | ☐ |
| M5 | 刷新率 | 观察屏幕 | 无明显闪烁 | ☐ |
| M6 | 亮度均匀 | 显示全白屏 | 各行亮度一致, 无暗行 | ☐ |
| M7 | 鬼影测试 | 交替显示全亮/全灭 | 切换时无残影 | ☐ |

> **M1 不亮排查**:
> 1. 检查 5V 供电 (P10 需要 5V, 不能用 3.3V)
> 2. 检查 PB3~PB8 接线 (特别是 OE 和 R1)
> 3. 检查 TIM2 中断是否正常 (在 ISR 中打断点)

### 3.2 按键测试

| 序号 | 测试项 | 操作 | 预期结果 | 通过? |
|------|--------|------|----------|-------|
| M8 | KEY1 (PC13) | 按下 KEY1 | 在调试器中检测到 KEY_1 事件 | ☐ |
| M9 | KEY2 (PA8) | 按下 KEY2 | 检测到 KEY_2 事件 | ☐ |
| M10 | KEY3 (PB11) | 按下 KEY3 | 检测到 KEY_3 事件 | ☐ |
| M11 | KEY4 (PB10) | 按下 KEY4 | 检测到 KEY_4 事件 | ☐ |
| M12 | 消抖 | 快速按 KEY1 | 只返回一次事件 | ☐ |
| M13 | 多键 | 同时按下 KEY1+KEY2 | 返回先按下的那个 | ☐ |

> **M8 不灵敏 (PC13)**: STM32F103 的 PC13 没有内部上拉, 需外加 10kΩ 上拉电阻

### 3.3 LoRa 模块测试 (主机)

| 序号 | 测试项 | 操作 | 预期结果 | 通过? |
|------|--------|------|----------|-------|
| M14 | SPI 通信 | 读取版本号 | 返回 0x12 | ☐ |
| M15 | 初始化成功 | 检查返回值 | `SX1278_OK` | ☐ |
| M16 | 接收模式 | 调用 `SX1278_StartRx()` | 正常进入接收 | ☐ |

---

## 4. 联调测试流程

> **测试环境**: 从机 + 主机同时上电, 300m 以内

### 4.1 通信建立

| 序号 | 测试项 | 操作 | 预期结果 | 通过? |
|------|--------|------|----------|-------|
| C1 | 首次连接 | 从机先上电, 再上电主机 | 主机屏幕 3 秒内显示温湿度数据 | ☐ |
| C2 | 数据格式 | 观察主机屏幕温度值 | 与室温一致 (手握 DS18B20 可验证) | ☐ |
| C3 | 湿度显示 | 观察主机屏幕湿度值 | 合理范围 (30~80%RH) | ☐ |
| C4 | 风扇状态 | 观察主机屏幕风扇显示 | 显示 "FAN:OFF" (默认关闭) | ☐ |
| C5 | 模式显示 | 观察主机屏幕模式显示 | 显示 "AUTO" (默认自动模式) | ☐ |

### 4.2 风扇控制

| 序号 | 测试项 | 操作 | 预期结果 | 通过? |
|------|--------|------|----------|-------|
| C6 | KEY1 开风扇 | 按主机 KEY1 | 从机继电器"嗒"一声, 主机显示 "FAN:ON" | ☐ |
| C7 | KEY1 关风扇 | 再按 KEY1 | 从机继电器"嗒"一声, 主机显示 "FAN:OFF" | ☐ |
| C8 | 模式切换 | 按主机 KEY2 | 屏幕在 AUTO/MANU 之间切换 | ☐ |
| C9 | 阈值加 | 按主机 KEY3 | THR 值 +1 (上限 50°C) | ☐ |
| C10 | 阈值减 | 按主机 KEY4 | THR 值 -1 (下限 20°C) | ☐ |
| C11 | 阈值边界 | KEY3 按到 50 后再按 | 不超过 50 | ☐ |
| C12 | 阈值边界 | KEY4 按到 20 后再按 | 不低于 20 | ☐ |

### 4.3 自动温控

| 序号 | 测试项 | 操作 | 预期结果 | 通过? |
|------|--------|------|----------|-------|
| C13 | 自动开 | 用吹风机加热 DS18B20 | 温度 ≥ 阈值后风扇自动开启 | ☐ |
| C14 | 自动关 | 移开吹风机 | 温度 ≤ (阈值-1°C) 后风扇自动关闭 | ☐ |
| C15 | 回差验证 | 观察开关温差 | 开和关的温度差约 1°C | ☐ |
| C16 | 手动优先 | 手动模式下改变温度 | 风扇状态不自动变化 | ☐ |

### 4.4 离线检测

| 序号 | 测试项 | 操作 | 预期结果 | 通过? |
|------|--------|------|----------|-------|
| C17 | 离线检测 | 断开从机电源 | 10 秒后主机显示 "ERR OFFLINE" | ☐ |
| C18 | 恢复在线 | 重新给从机上电 | 3 秒内主机恢复显示数据 | ☐ |

---

## 5. 完整功能测试

按照实际使用场景, 从头到尾走一遍:

```
测试步骤:
  1. 从机上电 → 观察继电器默认关闭
  2. 主机上电 → 屏幕显示 "TEMP CTRL V2.0"
  3. 等待 5 秒 → 主机显示温湿度数据
  4. 按 KEY3 3 次 → 阈值设为 33°C
  5. 用吹风机加热传感器 → 温度升到 33°C 后风扇自动启动
  6. 移开吹风机 → 温度降到 32°C 后风扇自动关闭
  7. 按 KEY2 → 切换到手动模式 (屏幕显示 MANU)
  8. 按 KEY1 → 手动开启风扇
  9. 按 KEY1 → 手动关闭风扇
  10. 按 KEY2 → 切回自动模式
  11. 拔掉从机电源 → 10 秒后主机显示离线
  12. 重新插上从机电源 → 自动恢复
```

---

## 6. 引脚冲突检查

### 6.1 从机引脚分配

| 引脚 | 功能 | 冲突检查 |
|------|------|----------|
| PA0 | DS18B20 DQ | ✅ 无冲突 |
| PA1 | DHT22 DATA | ✅ 无冲突 |
| PA4 | LoRa NSS | ✅ 无冲突 |
| PA5 | LoRa SCK (SPI1) | ✅ 无冲突 |
| PA6 | LoRa MISO (SPI1) | ✅ 无冲突 |
| PA7 | LoRa MOSI (SPI1) | ✅ 无冲突 |
| PB0 | LoRa DIO0 (EXTI0) | ✅ 无冲突 |
| PB1 | 继电器 IN | ✅ 无冲突 |

**结论: 从机引脚无冲突 ✅**

### 6.2 主机引脚分配

| 引脚 | 功能 | 冲突检查 |
|------|------|----------|
| PA4 | LoRa NSS | ✅ 无冲突 |
| PA5 | LoRa SCK (SPI1) | ✅ 无冲突 |
| PA6 | LoRa MISO (SPI1) | ✅ 无冲突 |
| PA7 | LoRa MOSI (SPI1) | ✅ 无冲突 |
| PA8 | KEY2 | ✅ 无冲突 (非 SPI 引脚) |
| PB0 | LoRa DIO0 (EXTI0) | ✅ 无冲突 |
| PB3 | HUB12 A (行选) | ✅ 无冲突 |
| PB4 | HUB12 B (行选) | ✅ 无冲突 |
| PB5 | HUB12 CLK | ✅ 无冲突 |
| PB6 | HUB12 STB (锁存) | ✅ 无冲突 |
| PB7 | HUB12 OE | ✅ 无冲突 |
| PB8 | HUB12 R1 (数据) | ✅ 无冲突 |
| PB10 | KEY4 | ✅ 无冲突 |
| PB11 | KEY3 | ✅ 无冲突 |
| PC13 | KEY1 | ✅ 无冲突 |

**结论: 主机引脚无冲突 ✅**

### 6.3 中断资源检查

| 中断源 | 优先级 | 使用者 | 冲突检查 |
|--------|--------|--------|----------|
| SysTick | 默认 | SysTick_Handler (1ms 时钟) | ✅ |
| EXTI0 | 抢占=1, 子=0 | LoRa DIO0 → SX1278 IRQ | ✅ |
| TIM2 | 抢占=2, 子=0 | HUB12 扫描刷新 | ✅ |

**结论: 中断资源无冲突 ✅**
- EXTI0 优先级 (1) 高于 TIM2 (2), 确保 LoRa 中断不被显示刷新打断
- SysTick 使用默认优先级, 不影响其他中断

---

## 7. 常见问题排查

### 7.1 编译错误

| 错误信息 | 原因 | 解决方案 |
|----------|------|----------|
| `hub75.h: No such file` | 旧代码仍引用 hub75 | 确认所有 `#include "hub75.h"` 已改为 `#include "hub12.h"` |
| `MODE_AUTO undeclared` | 缺少宏定义 | 确认 `app_master.h` 中包含 `#define MODE_AUTO` |
| `undefined reference to TIM2_IRQHandler` | 启动文件未包含 | 检查 startup_stm32f10x_md.s 是否在工程中 |
| `undefined reference to EXTI0_IRQHandler` | 同上 | 同上 |

### 7.2 硬件问题

| 现象 | 可能原因 | 排查步骤 |
|------|----------|----------|
| DS18B20 返回 -100°C | 未接 4.7kΩ 上拉 / 虚焊 | ① 万用表测 PA0 对 3.3V 阻值 ≈ 4.7kΩ ② 示波器看复位脉冲 |
| DHT22 读取失败 | 未接 10kΩ 上拉 / 时序不准 | ① 检查上拉电阻 ② 确认系统时钟 72MHz |
| LoRa 版本号不是 0x12 | SPI 接线错误 / 模块坏 | ① 逐一检查 PA4~PA7 ② 换一个模块测试 |
| LoRa 无通信 | 天线未接 / 频率不匹配 | ① 必须接天线 ② 两台设备用相同配置 |
| 继电器不动作 | GPIO 驱动不足 | ① 万用表测 PB1 ② 加三极管驱动电路 |
| P10 屏不亮 | 5V 未接 / OE 信号错误 | ① 确认 5V 供电 ② 用示波器检查 CLK 信号 |
| P10 屏闪烁 | TIM2 刷新率不对 | ① 检查 TIM2 预分频和周期 ② 确认中断正常触发 |
| P10 屏有暗行 | 地址线 A/B 接错 | ① 检查 PB3→A, PB4→B 接线 |
| KEY1 (PC13) 不灵敏 | 无内部上拉 | 外加 10kΩ 上拉电阻 |

### 7.3 软件问题

| 现象 | 可能原因 | 排查步骤 |
|------|----------|----------|
| 主循环卡死 | `while(1)` 等待初始化 | 在死循环处加 LED 闪烁, 定位卡在哪个初始化 |
| 显示内容错乱 | 帧缓冲越界 | 检查 `HUB12_SetPixel()` 的边界检查 |
| 按键连发 | 消抖时间不够 | 增加 `KEY_DEBOUNCE_MS` |
| 从机频繁重启 | 电源不稳 | 加大电容 (100μF + 100nF) 在 STM32 电源引脚旁 |
| 浮点运算慢 | 没有 FPU (Cortex-M3) | 可以用整数运算代替 (温度×10 存为整数) |

---

## 8. 测试记录表

### 从机测试记录

| 测试人 | 日期 | 从机编号 |
|--------|------|----------|
| | | |

| 序号 | 测试项 | 结果 | 备注 |
|------|--------|------|------|
| S1 | 电源 3.3V | ☐通过 ☐失败 | |
| S2 | 电源 5V | ☐通过 ☐失败 | |
| S3 | LED 指示 | ☐通过 ☐失败 | |
| S4 | DS18B20 初始化 | ☐通过 ☐失败 | |
| S5 | DS18B20 常温 | ☐通过 ☐失败 | |
| S6 | DS18B20 升温 | ☐通过 ☐失败 | |
| S7 | DS18B20 降温 | ☐通过 ☐失败 | |
| S8 | DS18B20 无传感器 | ☐通过 ☐失败 | |
| S9 | DHT22 读取 | ☐通过 ☐失败 | |
| S10 | DHT22 湿度响应 | ☐通过 ☐失败 | |
| S11 | DHT22 校验 | ☐通过 ☐失败 | |
| S12 | 继电器闭合 | ☐通过 ☐失败 | |
| S13 | 继电器断开 | ☐通过 ☐失败 | |
| S14 | 继电器默认状态 | ☐通过 ☐失败 | |
| S15 | 继电器 ON 电平 | ☐通过 ☐失败 | |
| S16 | 继电器 OFF 电平 | ☐通过 ☐失败 | |
| S17 | LoRa SPI 版本号 | ☐通过 ☐失败 | 返回值:______ |
| S18 | LoRa 初始化 | ☐通过 ☐失败 | |
| S19 | LoRa 接收模式 | ☐通过 ☐失败 | |
| S20 | LoRa 发送 | ☐通过 ☐失败 | |

### 主机测试记录

| 测试人 | 日期 | 主机编号 |
|--------|------|----------|
| | | |

| 序号 | 测试项 | 结果 | 备注 |
|------|--------|------|------|
| M1 | 屏幕点亮 | ☐通过 ☐失败 | |
| M2 | 全亮测试 | ☐通过 ☐失败 | |
| M3 | 字符显示 | ☐通过 ☐失败 | |
| M4 | 行显示 | ☐通过 ☐失败 | |
| M5 | 刷新率 | ☐通过 ☐失败 | |
| M6 | 亮度均匀 | ☐通过 ☐失败 | |
| M7 | 鬼影测试 | ☐通过 ☐失败 | |
| M8 | KEY1 | ☐通过 ☐失败 | |
| M9 | KEY2 | ☐通过 ☐失败 | |
| M10 | KEY3 | ☐通过 ☐失败 | |
| M11 | KEY4 | ☐通过 ☐失败 | |
| M12 | 消抖 | ☐通过 ☐失败 | |
| M13 | 多键 | ☐通过 ☐失败 | |
| M14 | LoRa 版本号 | ☐通过 ☐失败 | 返回值:______ |
| M15 | LoRa 初始化 | ☐通过 ☐失败 | |
| M16 | LoRa 接收 | ☐通过 ☐失败 | |

### 联调测试记录

| 测试人 | 日期 | 测试距离 |
|--------|------|----------|
| | | |

| 序号 | 测试项 | 结果 | 备注 |
|------|--------|------|------|
| C1 | 首次连接 | ☐通过 ☐失败 | |
| C2 | 温度显示 | ☐通过 ☐失败 | |
| C3 | 湿度显示 | ☐通过 ☐失败 | |
| C4 | 风扇状态 | ☐通过 ☐失败 | |
| C5 | 模式显示 | ☐通过 ☐失败 | |
| C6 | KEY1 开风扇 | ☐通过 ☐失败 | |
| C7 | KEY1 关风扇 | ☐通过 ☐失败 | |
| C8 | KEY2 模式切换 | ☐通过 ☐失败 | |
| C9 | KEY3 阈值加 | ☐通过 ☐失败 | |
| C10 | KEY4 阈值减 | ☐通过 ☐失败 | |
| C11 | 阈值上限 | ☐通过 ☐失败 | |
| C12 | 阈值下限 | ☐通过 ☐失败 | |
| C13 | 自动开风扇 | ☐通过 ☐失败 | 开启温度:____°C |
| C14 | 自动关风扇 | ☐通过 ☐失败 | 关闭温度:____°C |
| C15 | 回差验证 | ☐通过 ☐失败 | 回差:____°C |
| C16 | 手动优先 | ☐通过 ☐失败 | |
| C17 | 离线检测 | ☐通过 ☐失败 | |
| C18 | 恢复在线 | ☐通过 ☐失败 | |

---

*文档版本: v2.0 | 日期: 2026年3月*
